[package]
name = "map-deploy-review-contract"
edition = "2021"
version = "0.1.0"
description = "Deterministic MAP manifest deploy-review contract"
readme = "README.md"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
thiserror = "1"
